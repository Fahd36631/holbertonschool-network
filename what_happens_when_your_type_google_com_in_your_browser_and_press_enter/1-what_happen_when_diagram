```mermaid
flowchart LR
    A[User Browser<br/>types https://www.google.com] --> B[DNS Resolver]
    B --> C[DNS Resolution<br/>Returns Server IP]
    C --> D[TCP Connection<br/>Server IP:443]
    D -->|HTTPS / TLS Encrypted Traffic| E[Firewall]
    E --> F[Load Balancer]
    F --> G[Web Server]
    G -->|Dynamic Request| H[Application Server]
    H -->|Query Data| I[(Database)]
    I -->|Data Response| H
    H -->|Generate Web Page| G
    G -->|HTTPS Response| A
```